/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/haris_portfolio",
  assetPrefix: "/haris_portfolio/",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
